<% if user == current_user %>
  <span class='username'><%= user.username %> (you)</span>
<% else %>
  <span class='username'><%= user.username %></span>
<% end %>

<span class='email'><%= user.allowed_email %></span>

<span class='phone'><%= user.allowed_phone %></span>
